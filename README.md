# Clickbait_Detection
Clickbait Detection: This repository contains code for detecting clickbait headlines using Transformers. It provides implementations for building the detection model from scratch or utilizing pre-trained models.
